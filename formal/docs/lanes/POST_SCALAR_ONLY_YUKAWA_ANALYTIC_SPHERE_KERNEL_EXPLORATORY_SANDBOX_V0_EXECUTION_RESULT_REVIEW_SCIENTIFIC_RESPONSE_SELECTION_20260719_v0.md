# Post-analytic-sphere-kernel exploratory-sandbox failure scientific-response selection V0

## Decision

```text
SELECTED_FINAL_SERIALIZATION_CORRECTED_NON_DECISION_BEARING_SANDBOX_V1_EXECUTION
```

Selected route:

```text
AUTHORIZE_FINAL_SERIALIZATION_CORRECTED_NON_DECISION_BEARING_SANDBOX_IMPLEMENTATION_V1
```

The accepted V0 result review localized a final-aggregate preservation defect rather than
a formula, regression, derivative, boundary, mutation, runtime, or oracle disagreement.
The frozen contract was explicit, all eight execution stages were reached, the qualified
analytic oracle remains accepted, and no transient sandbox values are admissible.

The selector therefore authorizes one final versioned sandbox implementation and one
execution. Retirement or deferment remains the only alternative. No third sandbox,
contract repair, infrastructure prerequisite, or preservation-repair chain is available.

## Frozen two-way selection

| Candidate | Score | Disposition |
|---|---:|---|
| `ANALYTIC_SPHERE_KERNEL_EXPLORATORY_SANDBOX_V1_FINAL_ONCE` | 270 | selected |
| `RETIRE_OR_DEFER_ANALYTIC_REPLACEMENT_LANE` | 195 | runner-up |

The selected route remains first in `33 / 33` sensitivity variants. It preserves the
scientific implementation while correcting the one localized integration defect. Immediate
retirement is safe but would discard a proportionate final attempt whose failure was not
scientific or numerical.

## Exact V1 change boundary

The V0 source is frozen at:

```text
formal/python/tools/scalar_only_yukawa_analytic_sphere_kernel_exploratory_sandbox_v0.py
sha256: 27a32f540465ed78cb2094629033a4aa30e3142c1f75aa113fc88eb10c7563ae
```

V1 may change only:

- recursive conversion of permitted `Decimal` values to the frozen uppercase normalized
  decimal-string representation;
- a schema-complete serialization control that exercises the actual nested adjudication
  records and the complete final aggregate;
- strict rejection of unexpected object types and non-finite values;
- recursive schema validation of the normalized aggregate;
- canonical UTF-8 encoding with sorted keys and frozen separators;
- atomic temporary-file write, flush, `fsync`, strict parse/schema/hash verification, and
  atomic rename;
- V1 identifiers, filenames, source hashes, custody metadata, and narrowly necessary helper
  wiring or imports.

The implementation may not use `json.dumps(..., default=str)` or any equivalent permissive
fallback.

The analytic formulas, evaluator branches, eight regression cases, expected values,
derivative references, tolerances, thirteen probes, twelve mutations, predicates,
adjudicators, dependency scan, 10,000-call workload, interface semantics, and classification
precedence remain frozen. V1 must record the V0 source hash, its own source hash, and an exact
frozen-surface audit before any decision-bearing work begins.

## Real-path serialization control

Before the decision-bearing calculations, V1 must construct a schema-complete synthetic
final aggregate containing `Decimal` values in every decision-bearing location populated by
live adjudication. That exact structure must traverse the same normalizer, validator,
canonical encoder, atomic writer, strict parser, and hash verifier used for the final result.

The control must prove that no `Decimal` or other non-JSON-native object remains after
normalization. Failure consumes the execution and fails closed.

## One-shot and terminal rules

```text
sandbox V1 executions: exactly 1
automatic sandbox V2: prohibited
additional serialization-repair chain: prohibited
additional infrastructure prerequisite: prohibited
```

Outcome routing is exclusive:

```text
complete pass
→ independent exploratory result review only

complete preserved negative result
→ independent failure review and fresh substantive lane decision

another preservation or serialization failure
→ retire or defer the analytic replacement lane only
```

No outcome authorizes production replacement automatically.

## Hard firewalls

The V1 sandbox may not alter production code or dispatch, call or adjudicate the historical
cubature, conduct shadow qualification or adoption, rerun Stage A, compute torque, DFT
coefficients, the real-150 vector, a Jacobian, SVD, identifiability, or Stage B, or issue a
scientific or empirical claim.

The historical cubature may remain `UNADJUDICATED` and retired from decision-bearing use.

## What happened in this selection

No V1 implementation was created or executed by this selector. No V0 result was reconstructed.
The selector only consumes the accepted failure review and rotates authority to one final,
non-production, non-adjudicative implementation-and-execution target.

## Current authority

```text
execute_scalar_only_yukawa_analytic_sphere_kernel_exploratory_sandbox_v1_once
```
