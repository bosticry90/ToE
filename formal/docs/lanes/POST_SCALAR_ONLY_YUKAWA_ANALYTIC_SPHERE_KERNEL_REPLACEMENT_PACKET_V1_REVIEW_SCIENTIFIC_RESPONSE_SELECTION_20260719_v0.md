# Post-final-V1 analytic sphere-kernel replacement scientific-response selection

## Selected response

```text
route:
ISOLATE_KERNEL_REPLACEMENT_VALIDATION_INFRASTRUCTURE_PREREQUISITE

verdict:
SELECTED_KERNEL_REPLACEMENT_VALIDATION_INFRASTRUCTURE_PREREQUISITE_PREPARATION

candidate:
KERNEL_AGNOSTIC_VALIDATION_HARNESS_AND_SCHEMA_PREREQUISITE_V0
```

The selected prerequisite is not replacement packet V2. It is a separate,
kernel-agnostic infrastructure lane that may specify only the machinery needed
to make capability enforcement, typed predicates, mutation routing, dependency
scanning, and recursive result schemas independently executable.

## Ranking

```text
selected score: 240
runner-up score: 185
winning margin: 55
sensitivity: selected first in 30 / 30 variants
```

The runner-up retires replacement implementation while preserving the accepted
analytic oracle. Deferral and closure remain available, but they discard the
possibility that one reusable validation harness can resolve the remaining
non-physics blockers economically.

## Prerequisite boundary

The future packet may freeze:

- a process-scoped capability issuer and private-call protocol;
- typed numeric, exception, and relational predicate schemas;
- mutation call-route and adjudicator schemas;
- an AST/import/call-graph dependency-scanner contract;
- recursive canonical result schemas, enums, and duplicate-key parsing;
- kernel-free synthetic fixtures for qualifying that infrastructure.

It may not contain an analytic, Newtonian, or Yukawa kernel; edit V1; call the
accepted oracle on real cases; run real regression, boundary, or mutation
evidence; or change a production import, caller, or dispatch seam.

Completion cannot automatically reopen replacement implementation. Any packet
review or later infrastructure result must stop for another fresh selector.
V1 remains the final automatic replacement-contract repair.

No candidate kernel is created or executed by this selection. No V2, silent
correction, shadow qualification, cubature adjudication, Stage A rerun,
torque/DFT, identifiability, or Stage B work is authorized.

```text
current authority:
prepare_scalar_only_yukawa_kernel_replacement_validation_infrastructure_prerequisite_packet_v0
```
