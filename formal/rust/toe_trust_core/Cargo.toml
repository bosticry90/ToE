[package]
name = "toe_trust_core"
version = "0.1.0"
edition = "2021"
description = "ToE trust-core validator pilot"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
