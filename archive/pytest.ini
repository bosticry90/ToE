[pytest]
testpaths = tests field_theory/tests
python_files = test_*.py
addopts = -q -ra -m "not slow"
markers =
    slow: long-running tests
filterwarnings =
    ignore:`trapz` is deprecated:DeprecationWarning
